# TODO —— 推迟事项(YAGNI 停车场)

> 这里记的是**当前刻意不做、但以后可能要做**的事。每条都写清:是什么、为什么推迟、什么时候回来做。
> 原则:主线优先,别被这些勾走注意力;真到了该做的节点再翻这份清单。

## 多意图并行 / FAQ 逐子句 期间推迟的(2026-07-26)

- [ ] **FAQ 降级到 intent 级**(彻底解 `faqmix-05` 那条已知上限)
  - 现状:FAQ 逐子句匹配依赖分隔符(标点 / 换行 / 夹在汉字间的空格)。**完全无分隔符**的复合句切不开——"你们几点开门帮我查下订单12345" 整句相似度 0.81 仍会短路,第二问仍被静默丢弃。用例 `faqmix-05` 刻意留在评估集里量化这个缺口(预期失败),不藏。
  - 彻底解法:先 `decide_route` 拆 `intents[]`,再对**每个 intent 的 question** 单独查 FAQ。不依赖任何分隔符,无条件正确。
  - 为什么现在不做:FAQ 的全部卖点是"不惊动 LLM"(简历那句"降低简单问题进入 LLM 的比例")。降级后每次都要先付一次路由调用,实测全覆盖短路只要 **0.24s**,走 LLM 要 6s+,**差 100 倍**。当前不划算。
  - 什么时候回来做:①本地小模型就位(路由调用变廉价);②真实流量里发现无分隔符复合句常见。
- [ ] **`action-04` 偶发过拆**
  - 现象:"订单12345的火花塞我要退货,买错型号了" 偶尔被拆成 `action/-+chitchat/-`。单独打 `decide_route` **20 次全对**,是 `temperature=0` 下的抖动,不是稳定 bug。
  - 但危害不对称:路由掉 1 分是小事,发生时 chitchat 那半会往**真实退款回执**后面缝一句"请提供您的手机号或用户名以加快审核"——系统没有这个流程。`_merge_reply` 的"回执不可改写"铁律护住了回执,**没管住另一半在编什么**。
  - 两个候选修法:①路由 prompt 加判据(理由/情绪从句不构成独立处理单元);②结构性降噪(多意图时丢弃 chitchat intent)。倾向②更彻底。属核心路由逻辑,待作者定。
- [ ] **系统不知道"今天"是哪天 → 相对日期全办不了** ⭐(2026-07-26 被"你们几点营业，帮我约一下明天的保养"照出来)
  - 现象:`book_service` 只认 ISO 日期,而 `action_node` 的 system prompt **从没注入当前日期**。用户说"明天",LLM 只能 `ask_user` 反问"请问您说的明天具体是哪一天";用户答了"今天是 2026-07-27",它仍绕不过来,连追问两轮后放弃。「明天 / 后天 / 这周末」是最自然的说法,**目前全都办不了**。
  - 为什么一直没发现:评估集 `action-02/03/06` 清一色用显式 `2026-07-20` 这类 ISO 日期。**用例造得太规整**,比指标撒谎更隐蔽——这是"评估集本身也会有盲区"的新化身,值得写进面试话术。
  - 修法(一行):system prompt 注入 `今天是 YYYY-MM-DD(周X)`,相对日期让 LLM 自解析。属核心 prompt,待作者定。配套补 1-2 条相对日期评估用例("帮我约明天的保养")。
- [ ] **LLM 用文字追问,绕过 `ask_user` 机制**(同一个例子暴露,根源是上一条)
  - system prompt 明确写了"唯一能向用户要信息的方式是调用 ask_user 工具",但连续追问两轮后 LLM 改用**文字**问"请问明天具体的日期是哪一天呢?"。
  - 后果不只是难看:这句问话**没触发 interrupt** → `api.py` 返回 `status="done"` 而非 `pending_clarification` → **前端不进入"等补充"状态**,用户看到一个问题却没有对应交互态。
  - 是坑 6"LLM 的世界观=description"的延伸:光在 prompt 里禁止不够。修好上一条后大概率自然消失(不需要追问了),先观察再决定是否加硬约束(如检测到终局答案里带问号且本轮未 interrupt 就兜底转 ask_user)。
- [ ] **`agents.py:204` 调试 print 待删**:diagnosis 路径里查延迟留下的 `print(f"[{sub_q[:20]}] hyde=... retrieve=...")`,在服务进程里会一直往 stdout 打。
- [ ] **放弃澄清追问的边界**:用户被 `ask_user` 追问后不回答、直接发新问题时,那一轮的 qa 半边答案会滞留(merge 没跑过,清不掉)。实测 A/B 显示**当前不会污染下一轮**——因为残留停在未提交的 pending write 里,新一轮从已提交的 checkpoint 起跑。**但这个结论依赖"merge 是所有分支的唯一出口"**;哪天有分支绕过 merge 直连 END,清空会静默漏掉(merge 的触发不是 barrier,别的分支照样能唤醒它)。按 YAGNI 现在不为它建机制,记在这里。
- [ ] **入口重置清单有三处副本**:`api.py` 的 `/chat`、`lab_d10_3_eval.py` 的 `run_once`、`agents.py` 的 `__main__`(后者每题一个新 thread_id,不受影响)。`ticket_id` 那个 bug 就是新增 State 字段时漏抄清单造成的。真要治本是给"轮级字段"的归零做单一来源(如 `new_turn(question, **kw)` 工厂),但只有 3 个调用点,YAGNI;**再漏一次字段或出现第 4 个调用点就抽**。

## D5(图检索)期间推迟的

- [x] **Pydantic 结构化校验** ✅ D6 已落地
  - 在 `RouteDecision`(target/strategy 双字段 Literal 枚举)上启用,`model_validate_json` 解析+校验一步到位,ValidationError 兜底走 qa/knowledge。
  - 后续扩展点:Text2Cypher 的输出、D9 FastAPI 的请求/响应模型。

- [x] **图问题的拆解 / 路由** ✅ D6 已落地
  - `decide_route` 一次 LLM 调用同时决定 target(qa/action/chitchat)与 strategy(knowledge/compatibility/diagnosis),compatibility 走 GraphRetriever。

- [ ] **schema 自动导出**
  - 是什么:用 `CALL db.schema.visualization()` 自动生成图谱 schema 字符串,替代手写。
  - 为什么推迟:当前图小且固定,手写 schema 更清晰,还能顺手把业务规则(如年份区间判断)写进去,自动导出给不了这个。
  - 什么时候回来:节点/关系类型变多、手维护 schema 字符串变累时。

- [ ] **澄清追问 / slot filling(关键信息缺失时反问用户)**
  - 是什么:用户问"Ninja 400 用什么火花塞"没给年份时,反问"请问是哪一年的车?",拿到后再查;或"帮我查订单"没给单号时反问单号。
  - **实现方案(2026-07-20 作者定):用 interrupt 做澄清**——node(如 action)发现缺参数时 `interrupt({"问":"请问订单号?"})` 暂停,用户回 `Command(resume="12345")` **直接重放该 node、不过 supervisor 路由**。这顺带解决了"supervisor 路由裸回复缺口"(用户回一个光秃秃的'12345',若走新一轮 /chat 会被路由误判;走 resume 通道则直接送进欠账 node,压根不经过路由)。这是 interrupt 的甜区(对话内/同步/当场应答),不是"路由前改写"那种补丁。
  - 已知隐患:同"审批僵尸"一脉——系统分不清用户下一句是"答澄清"还是"改主意问别的"。澄清场景比退款轻(同用户/几秒/甜区),第一版可假设用户老实答,robust 处理"答非所问"是进阶。
  - robust 方案(作者 2026-07-20 设计):把澄清拆成独立 node,resume 进来先 LLM 分类"是不是答案"→ 条件边分流:是答案→回工具 node 填槽继续;改主意→交 supervisor 重路由("图多几条边")。注:HyDE 在此用不上(那是检索工具,填槽是分类问题,且订单号这类私有事实编不出)。
  - 更优的生产级解法:UI 层消歧(二期 React,仿 AskUserQuestion 给明确选项/回复框),从源头消除"答案 vs 新话题"歧义,后端不用猜;后端 LLM 分类留作纯 API/单聊天框退路。
  - 为什么仍推迟:①检索侧的指代缺口已由 qa 内改写(rewrite_with_history)闭环,是评估证明的;②路由侧裸回复缺口目前无失败样例(未证明),按数据说话先不做;③slot-filling 是特性不是 fix。
  - 当前的低成本替代:让 Cypher 一并返回 `year_from`/`year_to`,答案里自报"适用 2018–2023 年款",用户自证,不多花一轮交互。
  - 什么时候回来:二期,或评估集加一条"反问→裸回复"用例证明缺口后。

- [ ] **投诉检测阈值重新校准**
  - 是什么:`detect_complaint` 的 `threshold=0.65` 目前只用 11 条手造样本定的,大概率过拟合。
  - 当前依据:真投诉最低 0.734、中性提问最高 0.558,0.65 两边余量均衡。
  - 什么时候回来:D10 建评估体系时,用几十条真实分布样本重新画分数分布、重定阈值。同时补上召回率/误报率指标。
  - 附注:第一次尝试用 HF 情感模型(uer/roberta-base-finetuned-dianping-chinese)做第一层,实测在本领域完全失效——中性提问"机油多久换一次"负面分 0.991,高于真投诉 0.986,无任何阈值可分。原因是严重分布外(该模型训练于大众点评餐馆评论)。改用 BGE-M3 对投诉样例做语义匹配后分离度良好。

- [ ] **删掉 `classify_intent`(被 `decide_route` 取代后)**
  - 是什么:`decide_route` 一次返回 target+strategy,是 `classify_intent` 的升级版,两者功能重叠。
  - 状态更新(D6 收尾):rag.py 已删,`classify_intent` 在 app/ 内已无使用者,删除条件已满足。暂留一版,下次动 `query_processing.py` 时顺手删。

- [x] **action_node 从占位变成真 ActionAgent** ✅ D8 已落地
  - tools.py(mock 工具+Schema+高危名单)+ Function Calling 循环 + interrupt 审批(denied_tools 防重复申请)+ Reflection(≤3 次,驳回不反思)。
  - 遗留:投诉话术仍承诺"转人工"但无真实机制,待 D9 API 化时对齐(改"登记工单");多轮追问见下一条。

- [x] **多轮对话记忆(State 挂 messages 历史)** ✅ D9 已落地
  - `messages: Annotated[list, add_messages]` + 入口塞用户消息 + `_final()` 统一记账 + `_history()` 翻译层(LangChain 对象→openai dict,滑动窗口 n=10)。
  - 落地范围:**账本五路全记,读者只开通 action**(跨轮续办业务已验证:不重报订单号即可退款)。qa/supervisor 读历史见下面两条新 TODO。

- [x] **对话式 RAG:qa 路读历史(指代消解改写后再检索)** ✅ D10 已落地
  - `rewrite_with_history(question, history)`(query_processing.py):有更早上文(len>1)时用 LLM 把指代性问题改写成独立问题,再进检索;`len<=1` 直接原样返回(省 LLM 调用)。接在 qa_node 开头,knowledge/compatibility/diagnosis 三策略统一用改写后的 q。
  - 验证:multiturn-01#t2"那多久换一次"→改写成"Ninja 400 火花塞多久换一次"→命中"每12000公里"。评估唯一缺口闭环。
  - 踩坑:改写输出不能 `.lower()`(会毁精确型号 CPR8EA-9);prompt 要锁死"只输出问题本身"防解释污染检索;`if not history` 是死代码(本轮问题永远在 history 里),要用 `len<=1`。
  - 只做了 qa(评估证明的检索缺口);supervisor 路由裸回复缺口用 interrupt 澄清另解,见"澄清追问"条。

- [ ] **supervisor 路由读历史(裸回复场景)**
  - 是什么:agent 反问"请问订单号?"后用户光回一句"12345",decide_route 没有上下文会迷路(大概率误进 qa)。
  - 什么时候回来:与上一条同批;届时 decide_route 加最近几条历史作路由上下文。

- [ ] **ActionAgent 接入 MCP(工具动态发现进主流水线)**
  - 是什么:action_node 启动时经 MCP 客户端 `tools/list` 发现工具、`tools/call` 调用,替代本地 TOOLS_SCHEMA/TOOL_REGISTRY。
  - 为什么推迟(刻意决策):工具与 Agent 当前**同进程**,MCP 的互操作收益尚不存在;接入需在同步 action 循环里桥接异步客户端,且 D8 验证过的 interrupt/反思链路要全部重测——为不存在的收益付真实复杂度。协议层已在 lab 验证(lab_d9_1 服务器 + lab_d9_2 动态发现)。
  - 什么时候回来:二期 Spring Boot 业务系统独立部署时(工具真正住进另一个进程,HTTP 传输),或 D12 工程化若有余力。

- [ ] **图片型 PDF 的 OCR 接入(650 系手册)**
  - 现象:Ninja 650 保养手册(78 页)与 Z650RS 车主手册(174 页)是扫描件,pypdf 提取 0 页(全被 50 字符过滤器拦下,skipped 计数当场现形)。
  - 方案:①优先找文字型替代版本(明天补货时);②真要吃扫描件就上 OCR(PaddleOCR 中文友好),是独立一摊工程。
  - 什么时候回来:D10 补语料时先试①;②进二期。

- [ ] **章节切分的"页级近似":跨章混页(情况B)未处理**
  - 是什么:`build_section_map`(lab_d10_1_ingest.py)按书签起始页把**整页**归给一节。若某节从页**中间**才开始(情况B:X 节在页中结束、Y 节从同一页中间起),整页被判给 Y → 该页上半部分(X 的内容)被误贴成 Y 的章节标签。
  - 现状:当前 6 本手册章节几乎都从**页顶**开始,情况B未实际出现;P16/P21 那种"同页多书签"都是**同章父子/兄弟(情况A)**,靠空区间自动取最深子节,完全正确。所以是"已知但还没咬人"的近似。
  - 为什么推迟:修它要下沉到**字符级坐标**——用 pdfplumber 拿标题行的 y 坐标,在页内把文本按该 y 切两段分别归节,复杂度陡升;当前零失败样例,YAGNI。
  - 什么时候回来:换了没有"章节从页顶起"习惯的语料、或评估显示某页跨章污染时;动手前先写个"扫描非同章书签挤同页"的检测脚本证明缺口真实存在。

- [ ] **表格原子化的遗留(lab_d10_1_ingest.py 的 `_table_to_text` / `process_pdf`)**
  - 现状:有框线好表(`extract_tables` + 过滤:行≥3、列≥3、非空率≥0.4)已整表原子成块(实测 19 张,MT-07 保养表为标杆),单元格文字吃 `text_x_tolerance=1.5` 去粘;废表/误报判 None 后该页退回散文切窗。
  - 遗留①:命中表格的页**跳过了散文切窗**(`continue`),该页表格之外的脚注(如"*: 由授权店保养""**: 多尘路况减半周期")丢失。多数保养表脚注是次要信息,v1 接受;真要保留需按表格 bbox 裁掉表区、再对剩余正文切窗。
  - 遗留②:**跨页大表按页切开**(MT-07 保养表 P52-57 = 6 块)。好在每页表都重复了表头(里程列),各块自足可用;跨页拼表复杂度高,暂不做。
  - 遗留③:**CBR650R 保养表勾选标记 pdfplumber 抓不到**(源 PDF 用特殊字形/图元,非文本字符),整表非空率仅 16% 被过滤、退回散文;根治要 OCR/版面模型,不划算。
  - 什么时候回来:评估显示"保养周期/多久换 X"类查询命中差时;先加评估用例证明缺口再动手。

- [ ] **手写 DOCS 与官方手册的冲突治理**
  - 现象:DOCS 里"机油容量约 1.8 升"与官方手册 1.6/2.0L 并存,生成层如实呈现冲突并建议以官方为准(行为正确)。
  - 待办:D10 评估集收录"多源冲突"用例;长期需要数据权威级标注(官方手册 > 店家笔记)。

- [ ] **检索按 source 元数据过滤**
  - 是什么:问题里带车型时,Chroma 用 where={"source": ...} 只搜对应手册,精度暴涨(当前靠前缀文本进向量硬扛)。
  - 什么时候回来:D10 评估如果显示跨手册污染显著,立刻做;否则二期。

- [ ] **语料缺失时的降级启动**
  - 现象:agents.py 启动时读 data/manual_chunks.json,新 clone 的仓库没有这个文件(版权物不入库)会直接崩。
  - 修法:try/except 降级为仅 DOCS 启动 + 打警告;README 写清"跑 lab_d10_1/2 重建语料"。
  - 什么时候回来:D12 仓库公开前必须做。

## 二期工单化方案(整体架构,2026-07-19 讲解定稿)

**核心思路:审批从"对话的暂停键(interrupt)"→"独立工单(refund_tickets 表 + 状态机)"。** 对话线程发起工单后立刻正常结束,不挂起;商家在业务系统批复;结果异步推回对话。

**一笔退款的旅程:**
1. 用户"要退货"→ action_node 的 LLM 决定调 request_refund → 工具不再直接退款,而是**调业务系统开工单**(status=PENDING)→ 对话立刻回"已提交单号T001,审核后通知"→ **这句进账本(有始有终)**→ 线程干净结束。
2. 用户继续插话 → 账本里退货已有下文 → LLM 不复活它 → 僵尸死。
3. 商家控制台看 PENDING 工单 → 通过/驳回 → 更新工单状态 → 通过则**工单系统确定性执行退款(无 LLM)**→ 结果推回对话。

**四坑一次清(同一病根=审批寄生对话线程):** 僵尸复活 / 商家 yes 蒸发 / 审批锁会话 / /approve 裸奔。

**三层架构:**
- React:用户聊天窗 + 商家控制台(待审工单列表 + 通过/驳回)
- FastAPI(Agent 层):对话/路由/检索;request_refund 工具改为"调业务系统开工单"(接口契约不变,mock→真实,action_node 几乎不动)
- Spring Boot(业务系统):refund_tickets 表 + 状态机 + 鉴权 + 执行退款 + 通知(业务规则唯一权威)

**推送方式(二期开工时定):** 轮询(最简,客服场景够用)vs WebSocket/SSE(实时,多维护长连接)。待定。

**范围锁死:** 只把现有退款审批工单化跑通,不铺 VISION 新功能(那是三期)。目标:一期二期下周内完成可实用客服。

**interrupt 的定位认知(面试素材):** interrupt 适合"单事务的同步中途卡点"(D8 单测完美);不适合"长时间可打断、与对话并行的审批"——后者正确工具是工单+状态机+异步通知。知道工具适用边界 > 会用工具。

---

## 二期记忆架构蓝图(2026-07-20 讨论定,详见 REVIEW.md 9.5)

**"记忆"拆四层,各住各地——别混成一个 DB:**
| 层 | 是什么 | 现在 | 二期存哪 | 键 |
|---|---|---|---|---|
| ①会话内工作记忆 | 当前对话 messages | MemorySaver(内存,重启丢) | 持久 checkpointer:Postgres/Redis | thread_id |
| ②跨会话长期记忆 | 用户车型/偏好/历史订单/过往摘要 | 无 | 结构化→Spring Boot 业务库;语义→Chroma(按 user_id 过滤) | user_id |
| ③领域知识 | 手册/配件 | Chroma+Neo4j | 维持 | 全局 |
| ④长上下文 | 对话塞不进窗口 | 滑窗 n=10 | 加摘要压缩/对历史检索 | — |

**两把刀(别混):** DB 治"存不下/重启丢";context engineering(滑窗/摘要/对历史检索)治"喂给 LLM 太长"。DB 存百万条也喂不进 LLM——存全量、喂精选,两件独立的事。逼你上 DB 的是"多用户+不丢",不是单段对话变长。

**LangGraph 原生分工:** Checkpointer=短期(线程内,thread_id)/ Store(langgraph.store)=长期(跨线程,user_id 命名空间,部分支持语义检索)。②这层二期用 Store 或业务库实现。

**落地顺序(YAGNI,现在别做):** ①换持久 checkpointer(和工单化同批,重启不丢挂起审批)→ ②用户系统就绪后加跨会话记忆 → ④对话真长了再上摘要压缩。现在 MemorySaver+n=10 够用。

---

- [ ] **【产品决策已定】审批不挡路:审批期间会话照常(立场 B,作者 2026-07-19 拍板)**
  - 决策:用户提交高危审批后,应能在同一会话继续做别的(逛配件、补单、找替代品),不被"审批挂起"锁死会话。**不选立场 A(锁会话)**——那不符合真实客服体验。
  - 这要求审批彻底脱离对话线程:审批 → 后台工单(独立状态机)→ 商家批复 → 结果主动推回对话(作为新消息)。对话流与审批流解耦。
  - 连带修掉:僵尸退款复活(记忆×中止交互坑)、重放非确定性、商家 yes 蒸发、/approve 鉴权——同一病根(审批寄生对话线程),工单化一次性解决。
  - 落地:二期 Spring Boot 业务系统 + 工单表 + 推送。**目标:一期二期下周内全部完成,做出可实际应用的客服。**
  - 面试价值:D13 讲稿必收——"我用一个实验发现审批寄生对话线程的脆弱性,并据此做了产品与架构决策"。

- [ ] **interrupt 与 LLM 同居一节点:重放非确定性风险(复习日实验实锤,2026-07-19)**
  - 实验:s9 退款挂起 → 同 session 插问"查订单12346" → 旧 interrupt 被新一轮作废,但 LLM 从 messages 账本里"复活"退款、产生新 interrupt(payload 的 user_question 已变为新问题,铁证);商家此时 /approve yes → 200 done,但重放时 LLM(temp=0 仍漂移)改走"查单+反问退款原因"路径,未再调 request_refund → interrupt 未被执行,yes 无人消费,**退款未执行且零报错**。
  - 根因:interrupt 的"确定性重放"承诺被重放路径上的 LLM 调用破坏(工作流引擎要求重放代码确定性,LLM 天然不满足)。
  - 修法:①把审批拆进无 LLM 的确定性小节点(工具调用决策先提交 State,审批节点只做 interrupt+执行已锁定调用);②或审批工单化与对话流解耦(二期 Spring Boot 方案,与 /approve 鉴权同批)。另:/chat 对"pending 中插新话"应有明确策略(提示挂起审批存在)。
  - 什么时候回来:二期业务系统设计时;D13 面试讲稿必收此实验(设计实验→证伪预测→发现脆弱性→架构结论,完整方法论闭环)。

- [ ] **/approve 端点鉴权(作者复习日自己发现的真漏洞)**
  - 现象:/approve 无任何认证,用户拿着自己的 session_id 就能自批退款(POST /approve decision=yes),HITL 闸门形同虚设。
  - 辨析:诱导 LLM 说 yes 是打不穿的(approval 的数据源只有 Command(resume),与 LLM 输出通道不相交);真正的洞在端点裸奔。
  - 修法:认证 + 角色授权(商家角色才能调 /approve),用户端/商家端两个受众分离;审批操作留审计日志。
  - 什么时候回来:D12 工程化或二期(Spring Boot 网关做鉴权是自然位置)。

- [ ] **MemorySaver 换持久化 checkpointer(Redis 在本项目最该用在这)**
  - 是什么:当前 checkpoint 在 uvicorn 进程内存里,重启服务=所有会话(含挂起中的澄清)蒸发。
  - 为什么优先级上来了:二期起了多进程(FastAPI + Spring Boot),真多实例部署时会话状态要跨实例共享,内存 checkpoint 各进程一份不共享。
  - 方案:PostgresSaver(已有 Postgres,一库多用)或 RedisSaver;顺带解决多实例会话共享 + 重启不丢挂起。
  - **面试锚点**:Redis 在本项目的正确用武之地是这个(持久化/共享会话),**不是"FAQ 缓存"**——FAQ 只十几条,内存 numpy 匹配本身就是缓存角色,再上 Redis 是过度设计。知道工具边界 > 堆技术栈。
  - 什么时候回来:二期做用户系统/多实例部署时。

- [ ] **FAQ 存储升级(Redis 缓存 / 挪进向量库)—— 目前刻意不做**
  - 现状:FAQ = 内存字典(十几条)+ BGE-M3 numpy 余弦匹配,`_faq_vectors` 模块加载时算好、常驻内存。既不是 Redis,也不是 Chroma(Chroma 存手册 RAG 语料,与 FAQ 两回事)。
  - 为什么不做:十几条数据内存算余弦几毫秒,内存字典本身就等于缓存。区分标准:少量临时→numpy,大量持久→向量库。
  - 什么时候回来:①FAQ 涨到成百上千条或要 TTL 过期 → 挪进向量库/Redis;②多实例部署需共享 FAQ 缓存 → Redis。当前单实例十几条,YAGNI。

- [ ] **拆出 `models.py` / `schemas.py`**
  - 是什么:把 Pydantic 模型(目前只有 `RouteDecision`)集中到独立文件。
  - 为什么推迟:目前只有一个模型、一个使用者,就近放在 `query_processing.py` 里可读性更好。
  - 什么时候回来:模型多到 3-5 个,或出现跨文件复用时;D9 做 FastAPI 请求/响应模型时会自然触发,和 `config.py` 集中、db 层抽取一起做。

- [ ] **FAQ 误拦截"办理类请求"(加 LLM 意图确认层?)**
  - 现象(D8 实测):"订单12345这个东西我不想要了,退货"(要**办理**退货)被 FAQ 语义匹配劫走,返回了退货**政策**——答非所问,且 action/HITL 流程全被绕过。
  - 根因:语义相似度**认话题不认意图**("我要退货"和"退货政策是什么"话题重合)——与情绪层"认话题不认褒贬"(服务真好 vs 服务真差)是同一盲区的两副面孔。
  - 当前修法(免费):FAQ 样例措辞全部政策问句化("退货政策是什么/怎么退货/退货有什么条件"),删掉与办理语气接近的样例。
  - 未来方案:复用情绪拦截的**双层模式**——FAQ 语义命中后加一层轻量 LLM 判断"问信息 or 要办事",是办事则放行给 decide_route。代价:FAQ 命中从 0 次 LLM 变 1 次(仍比全链路便宜)。折中:仅在相似度模糊带(如 0.75-0.85)触发确认,高分段直接放行。
  - 什么时候回来:D10 评估集必须包含"FAQ 误拦率"指标;数据说明误拦真实存在再上这层,别拍脑袋加。

- [x] **checkpoint 反序列化白名单警告(RouteDecision)** ✅ D9 已修
  - 采用方案②:State 不存富对象——decision 经 `model_dump()` 脱壳成 dict 再入 State,Pydantic 只在 LLM 输出边界校验(序列化边界只过数据不过行为,同 Java 的 DTO 纪律)。警告消除。
  - 同批完成:投诉话术与真实能力对齐("转接人工"→"登记工单,人工客服尽快跟进")。

- [ ] **只读的数据库级兜底(Neo4j 只读权限用户)**
  - 是什么:给数据库开一个只有读权限的账号,让数据库本身拒绝写操作,而不只靠应用层关键词扫描。
  - 为什么推迟:应用层 `WRITE_PATTERN` 正则扫描当前已够用。
  - 什么时候回来:项目工程化 / 部署(D9 或 D12)做纵深防御时。

- [ ] **登录页"遮罩揭示"视觉(前端打磨,2026-07-24 作者定稿)**
  - 想要的效果:**纯黑背景**,一辆**大号白色摩托剪影从右向左驶过**;登录表单**只在白色剪影覆盖处显形,黑色区域不可见**——摩托开过时把表单"擦"出来。本质=那个 404 创意特效的核心机制("白形状扫过、内容只在白处可见"),但换成一辆干净的大摩托,不要一堆小东西乱飞。
  - 实现路子:用 CSS `mask-image`(或 SVG `<mask>` / `mix-blend-mode`)——把移动的白色摩托 SVG 当作**遮罩源**(白=透出、黑=遮住),表单 clip 到白区;摩托用 `@keyframes` 从 `translateX(110vw)` 移到 `-40vw`。关键是**遮罩层与内容层对齐同一坐标系**。
  - 素材教训(2026-07-24):第一版手绘的摩托/头盔 SVG 太糙被否。回来做前先备**一张干净的大摩托侧面剪影矢量图**(找现成 CC0 素材或让设计出),别再手撸 path。
  - 为什么推迟:纯视觉,非功能主线;当前普通登录卡完全够用。
  - 什么时候回来:二期前端打磨阶段,或录简历 demo 想加记忆点时。

- [x] **节点重放会重复开退款工单** ✅ 2026-07-25 已修(review 发现,同日修复)
  - 是什么:`action_node` 里 `request_refund` 开工单是**真实写操作**,而它可能排在 `ask_user`(interrupt)**之前**。interrupt 恢复 = 节点从头重放 → 重放时 `request_refund` 再执行一次 → 同一订单开出**两张 PENDING 工单**(原 `TicketServiceImpl.open()` 无任何去重守卫,无脑 `repo.save`)。
  - 触发场景:复合请求"退订单 12345,顺便约个保养" —— 退款先开单成功,预约缺日期 → `ask_user` → interrupt → 用户补答 → 节点重放 → 第二张工单。
  - 这是**坑 6 的第二化身**:当年的教训是"真正的写操作必须放在 interrupt 之后";工单化之后写操作换了个名字(执行退款→开工单)、interrupt 换了用途(审批→澄清追问),**位置却跑到了挂起点前面**,老约束被悄悄违反。重构后没人会去复查一条针对旧代码写的规则。
  - **为什么幂等只能落在业务系统**:agent 侧记不住任何事 —— 被 interrupt 打断时节点根本没 `return` 过,state 补丁没提交、局部变量随重放清零。**能跨重放记住事情的,只有重放管不到的地方**,也就是数据库。这与"业务规则唯一权威在业务系统"是同一条原则。
  - **采用的修法(硬版)**:`config/SchemaGuards`(新建)启动时建**部分唯一索引** `uniq_pending_ticket_per_order ON refund_tickets(order_id) WHERE status='PENDING'`。条件必须带 `WHERE status='PENDING'` —— 工单被驳回后用户应该还能再申请,约束要精确表达"同订单同时只能有一张待审",而不是"一辈子只能退一次"。JPA 的 `@Table(indexes=...)` 表达不了带条件的索引,故走 JdbcTemplate 原生 DDL(`IF NOT EXISTS` 幂等,同 DataSeeder 纪律)。`TicketServiceImpl.open()` 配套改幂等:快路径查已有待审单直接返回,`save` 撞约束则回头取先到者那张。
  - **两个 Java 陷阱(面试素材)**:①`open()` 上**必须拿掉 `@Transactional`** —— 同一事务里撞约束后事务被标记 rollback-only、Postgres 连接进入 aborted 状态,`catch` 里再查库直接报 `current transaction is aborted`;不加事务时 `save` 走 Spring Data 自带那层事务、失败独立回滚,`catch` 的查询是全新事务才看得见别人已提交的行。②别想着抽个 `@Transactional(REQUIRES_NEW)` 私有方法 —— **Spring 事务靠代理,`this.xxx()` 自调用不过代理**,注解等于没写。③附带细节:`@GeneratedValue(IDENTITY)` 逼 Hibernate 在 persist 时立刻 INSERT(要取生成的 id),所以约束冲突在 `save()` 当场抛出而非拖到 commit,`catch (DataIntegrityViolationException)` 才接得稳。
  - **实测**:①启动时库里有 9 张 12345 的 PENDING(历次调试积压)→ 索引建不上,SchemaGuards **fail-loud 报清订单号/张数/可直接执行的清理 SQL,且不拖垮服务**(照常起来);②归档重复单后索引建立成功;③已有待审单的订单再开 → 返回原单号;④**8 个并发请求同时开同一订单 → 7 个撞上唯一约束(SQLState 23505)、库里只落 1 行、8 个客户端全部拿到同一个单号**。这条证明了"先查后插"单独用是错的:8 个请求全部穿过了那一查。

- [ ] **MCP 工具签名的标注与默认值不一致(None bug 的病根)**
  - `tool-service/tools.py` 的 `request_refund(..., session_id: str = None, user_id: str = None)`:**标注写 `str`、默认给 `None`**。fastmcp 只看标注生成 JSON Schema,于是 schema 声明这两个是必填 `str`,显式传 `None` 会被 Pydantic 在**调用发出之前**拒掉(表现:业务系统日志干干净净,像调用从没发生过)。
  - 已用 agent 侧 `state.get("session_id") or ""` 兜住(2026-07-25 commit),但**病根在标注**。正解:改成 `Optional[str] = None`,让 schema 如实反映"可以没有"。
  - 什么时候回来:下次动 tool-service 时顺手改,改完要重跑一遍退款链路(schema 变了)。

- [ ] **评估集的两处盲区(自己发现的"指标会撒谎")**
  - ①**记忆型提问无用例**:2026-07-25 修的"我骑的是什么 → chitchat 读账本直答"目前**没有回归保护**,下次谁动路由 prompt 都可能悄悄弄坏它。补 2-3 条(陈述车型 / 隔轮回问 / 问历史里没有的东西看会不会编)。
  - ②**action 用例没有 `expected_points`**:所以 `hit` 是 null,评估**只验路由不验答案**。后果已经真实发生过 —— 三条退款因 None 参数全部失败,评估照样报 46/46 全绿。给 action 用例加要点(如 `["工单"]` / `["超出", "7 天"]`),让"到底办成没有"进指标。
  - ③`高危审批触发 x/2` 这个指标已过时:退款工单化后 interrupt 只服务澄清追问,那两条命中的其实是 `ask_user`。改名成「澄清追问触发」或直接换成「工单开出率」。

- [ ] **路由 prompt 的措辞小瑕疵(等下次改路由时一起做)**
  - `decide_route` 里写着"优先判据(先判**这两条**...)",底下挂了 **3 个** bullet(第三条其实是补充说明不是判据)。数字对不上会让 LLM 犯迷糊。
  - 为什么现在不改:**当前这版 prompt 是跑过三轮完整评估、逐条 diff=0 验证过的基线**,改一个字基线就作废。等下次真要动路由 prompt 时,连同重跑评估一起做。

---

> 项目级的长期 TODO 另见 CLAUDE.md 第 8 节(FAQ 增删改查、LLM 客户端解耦、向量库规模化等)。这份文件专收「随做随记」的推迟项。
