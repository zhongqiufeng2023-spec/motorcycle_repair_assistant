# 版本锁定自实际跑通的环境(2026-07)。Python 3.11+。
# 注意(坑记录):FlagEmbedding(BGE-M3)与 sentence-transformers(BGE-reranker)对 transformers
# 版本要求曾冲突,当前这组版本是实测共存的组合,升级 transformers 前先在小样本验证。

# 大模型调用
openai==2.45.0
python-dotenv==1.2.2
langgraph==1.2.9
langchain==1.3.13
langchain-deepseek==1.1.0
langsmith==0.10.2            # wrap_openai 全链路可观测

# 检索增强(RAG)
chromadb==1.5.9
FlagEmbedding==1.4.0         # BGE-M3 稠密向量
sentence-transformers==5.6.0 # BGE-reranker-v2-m3(CrossEncoder 加载,避开与 M3 的版本死锁)
transformers==5.13.1         # 版本敏感,见顶部说明
rank-bm25==0.2.2
jieba==0.42.1
numpy==2.5.1

# 图数据库(配件兼容查询)
neo4j==6.2.0

# 语料入库(PDF 手册提取)
pypdf==6.14.2               # 读书签大纲(outline)做章节感知切分
pdfplumber==0.11.10         # 懂版面:extract_tables 抓有框线表格 + extract_text(x_tol)去字符粘连

# 服务化(API 后端)
fastapi==0.139.2
uvicorn==0.51.0

# MCP(工具协议封装)
fastmcp==3.4.4
